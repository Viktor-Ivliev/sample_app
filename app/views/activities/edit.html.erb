<% if signed_in? %><% provide(:title, "Редактирование активность" ) %>
    <h1 class="zagolovki">Редактировать активность</h1>
    <div class="row">
      <div class="span6 offset3">
        <%= render 'form' %>
      </div>
    </div>
<%end %>